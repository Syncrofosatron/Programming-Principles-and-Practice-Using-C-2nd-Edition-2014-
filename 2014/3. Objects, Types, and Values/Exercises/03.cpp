#include <iostream>

int main()
{
    int double h = 0;  // two or more data types in declaration of 'h'.
    float &$ = 1;  // cannot bind non-const lvalue reference of type 'float&' to a value of type 'int'.
    int %%% = 2;  // expected unqualified-id before '%' token.
    char $ = 'x'; // conflicting declaration 'char $'.
    char friend = 'v';  // error: 'friend' used outside of class.
    double _perc = 79;
}